type Account @entity {
  "Account address"
  id: ID!
  balance: BigInt!
}

type Transfer @entity {
  id: ID!
  amount: BigInt!
  from: String!
  to: String!
  extrinsicIndex: Int!
  extrinsicHash: String!
  blockNumber: Int!
  success: Boolean!
  createdAt: DateTime!
}
